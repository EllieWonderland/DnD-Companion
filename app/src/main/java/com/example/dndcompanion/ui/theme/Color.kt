package com.example.dndcompanion.ui.theme

import androidx.compose.ui.graphics.Color

// === Pergament & Stein – Neue Farbpalette ===

// Pergament-Töne (Hintergrund & Karten)
val Pergament = Color(0xFFF2E6C9)          // Warmes Pergament-Beige
val PergamentDunkel = Color(0xFFD4C4A0)    // Abgedunkelter Rand / Schatten
val PergamentHell = Color(0xFFFAF3E3)      // Leicht aufgehelltes Pergament

// Wald & Natur (Header, Regelwerk)
val Waldgruen = Color(0xFF2E5339)          // Tiefes Waldgrün
val WaldgruenDunkel = Color(0xFF1B3526)    // Sehr dunkles Grün (StatusBar)
val WaldgruenHell = Color(0xFF4A7C59)      // Etwas helleres Grün für Hover

// Akzentfarben
val OchsenblutRot = Color(0xFF8B2500)      // Warmes Ochsenblutrot
val OchsenblutRotHell = Color(0xFFB33A00)  // Helleres Rot für Hover

// Metall-Töne (Buttons, Rahmen)
val Bronze = Color(0xFF9C7A3C)             // Warmes Bronze
val BronzeHell = Color(0xFFBE9B5C)         // Helles Bronze (Hover)
val BronzeDunkel = Color(0xFF7A5C2A)       // Dunkles Bronze (Schatten)
val EisenGrau = Color(0xFF6B6B6B)          // Für deaktivierte Elemente

// Charakter-spezifische Akzente
val WaldGold = Color(0xFFC9A84C)           // Athania / Ranger – warmes Gold
val WaldGoldHell = Color(0xFFE0C56E)       // Helleres Gold
val HexenLila = Color(0xFF391F4F)          // Delat / Warlock – mystisches Lila
val HexenLilaHell = Color(0xFF5A3D80)      // Helleres Lila

// Spezialfarben
val TempHPBlau = Color(0xFF5CACEE)         // Eisblau für temporäre HP
val TodRuneRot = Color(0xFFCC3333)         // Rote Rune (Todesrettungs-Misserfolg)
val TodRuneGruen = Color(0xFF339933)       // Grüne Rune (Todesrettungs-Erfolg)
val TintenSchwarz = Color(0xFF1A1409)      // Tinten-Schwarz für Text auf Pergament
val TintenBraun = Color(0xFF3D2B1F)        // Dunkelbraun für Sekundärtext